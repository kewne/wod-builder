# WOD Builder

A browser app for building WODs (Workout Of the Day).

This is also a code story, which you can follow [here](https://kewne.github.io/wod-builder/).

To run for development: `npm run dev-server`.